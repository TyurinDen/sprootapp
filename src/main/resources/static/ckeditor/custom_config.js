CKEDITOR.editorConfig = function (config) {
    config.removePlugins = 'about, info, pasteFromWordRemoveFontStyles, pasteFromWordCleanupFile, ' +
        'pasteFromWordPromptCleanup, clipboard, pastetext, pastefromword';
    // config.preset = 'standard';
    // config.language = 'en';
    // config.uiColor = '#AADC6E';
};